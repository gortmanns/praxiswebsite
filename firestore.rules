/**
 * @fileOverview Firestore Security Rules for the application.
 *
 * Core Philosophy:
 * This ruleset prioritizes a secure-by-default posture, granting broad read access where appropriate while strictly controlling write access based on role-based authorization.
 *
 * Data Structure:
 * - `/doctor_profiles/{doctorId}`: Stores publicly accessible doctor profile information.
 * - `/contact_form_submissions/{submissionId}`: Stores sensitive contact form submissions, accessible only to administrators.
 * - `/roles_admin/{userId}`:  A collection that marks users as admins. The existence of a document with a user's ID grants admin access. The content of this document is ignored.
 *
 * Key Security Decisions:
 * - Doctor profiles are publicly readable to all authenticated users.
 * - Contact form submissions are strictly restricted to admin users.
 * - Admin status is determined solely by the presence of a document in the `/roles_admin/{userId}` collection.
 * - No user listing is allowed for the `/roles_admin/{userId}` collection.
 * - Data validation is relaxed in favor of rapid prototyping. Only authorization-critical data is validated.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Allows read access to doctor profiles for all authenticated users, but restricts writes.
     * @path /doctor_profiles/{doctorId}
     * @allow (get, list) User is authenticated.
     * @deny (create, update, delete) User is not an admin.
     * @principle Public read, restricted write access.
     */
    match /doctor_profiles/{doctorId} {
      allow get, list: if isSignedIn();
      allow create, update, delete: if isAdmin();
    }

    /**
     * @description Restricts access to contact form submissions to admin users only.
     * @path /contact_form_submissions/{submissionId}
     * @allow (get, list) User is an admin.
     * @deny (get, list) User is not an admin.
     * @allow (create, update, delete) User is an admin.
     * @deny (create, update, delete) User is not an admin.
     * @principle Role-based access control.
     */
    match /contact_form_submissions/{submissionId} {
      allow get, list: if isAdmin();
      allow create, update, delete: if isAdmin();
    }

     /**
      * @description Determines admin status based on the existence of a document in this collection. No listing allowed.
      * @path /roles_admin/{userId}
      * @allow create: if isOwner(userId);
      * @deny create: if !isOwner(userId);
      * @allow get: if isAdmin();
      * @deny get: if !isAdmin();
      * @allow list: if false;
      * @deny update, delete: if true;
      * @principle Role-based access control through document existence.
      */
    match /roles_admin/{userId} {
        allow get: if isAdmin();
        allow list: if false;
        allow create: if isOwner(userId);
        allow update: if false;
        allow delete: if false;
    }

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    function isAdmin() {
        return exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid));
    }
  }
}