/**
 * @file Firestore Security Rules
 * @generated
 * @version Prototype
 *
 * @description This ruleset allows completely open access to all data, as explicitly requested.
 *   All reads and writes are permitted without authentication or authorization checks.
 *   This configuration is suitable for prototyping and disables all security restrictions.
 *
 * @security WARNING: This ruleset provides NO SECURITY. It should NEVER be used in a production environment.
 *   All data is publicly readable and writable.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Allows anyone to read or write doctor profiles.
     * @path /doctors/{doctorId}
     * @allow (get, list, create, update, delete) Any user, authenticated or not.
     * @deny None. All requests are allowed.
     * @principle Disables all security restrictions for prototyping.
     */
    match /doctors/{doctorId} {
      allow get, list: if true;
      allow create, update, delete: if true;
    }

    /**
     * @description Allows anyone to read or write staff member profiles.
     * @path /staff/{staffId}
     * @allow (get, list, create, update, delete) Any user, authenticated or not.
     * @deny None. All requests are allowed.
     * @principle Disables all security restrictions for prototyping.
     */
    match /staff/{staffId} {
      allow get, list: if true;
      allow create, update, delete: if true;
    }

    /**
     * @description Allows anyone to read or write medical partner information.
     * @path /medicalPartners/{partnerId}
     * @allow (get, list, create, update, delete) Any user, authenticated or not.
     * @deny None. All requests are allowed.
     * @principle Disables all security restrictions for prototyping.
     */
    match /medicalPartners/{partnerId} {
      allow get, list: if true;
      allow create, update, delete: if true;
    }

    /**
     * @description Allows anyone to read or write other partner information.
     * @path /otherPartners/{partnerId}
     * @allow (get, list, create, update, delete) Any user, authenticated or not.
     * @deny None. All requests are allowed.
     * @principle Disables all security restrictions for prototyping.
     */
    match /otherPartners/{partnerId} {
      allow get, list: if true;
      allow create, update, delete: if true;
    }

    /**
     * @description Allows anyone to read or write practice holiday periods.
     * @path /holidays/{holidayId}
     * @allow (get, list, create, update, delete) Any user, authenticated or not.
     * @deny None. All requests are allowed.
     * @principle Disables all security restrictions for prototyping.
     */
    match /holidays/{holidayId} {
      allow get, list: if true;
      allow create, update, delete: if true;
    }

    /**
     * @description Allows anyone to read or write banner settings.
     * @path /settings/banners
     * @allow (get, list, create, update, delete) Any user, authenticated or not.
     * @deny None. All requests are allowed.
     * @principle Disables all security restrictions for prototyping.
     */
    match /settings/banners {
      allow get, list: if true;
      allow create, update, delete: if true;
    }
  }
}